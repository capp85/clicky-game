import React from "react";
import "./style.css";

const Header = props => <h1 className="header">{props.children} Try to click on each character, but don't hit any duplicates or Your Sunk!</h1>


export default Header;
